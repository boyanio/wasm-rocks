describe("rockstar", () => {
  describe("parser", () => {
    describe("if", () => {
      // it("parses if statement with a variable condition", () => {
      //   const program = `
      //   If Tommy is nobody
      //   Shout "FizzBuzz!"
      //   `;
      //   const { statements } = parse(program);

      //   expect(statements.length).toEqual(2);
      //   expect(statements[0].type).toEqual("if");

      //   const ifStatement = statements[0] as IfStatement;
      //   expect(ifStatement.condition.type).toEqual("condition");

      //   const condition = ifStatement.condition as LogicalExpression;
      //   expect(condition.operator).toEqual("equals");
      //   expect(condition.left).toEqual({
      //     type: "variable",
      //     name: "tommy"
      //   });
      //   expect(condition.right).toEqual({
      //     type: "null"
      //   });

      //   expect(statements[2].type).toEqual("say");
      // });
      it("TODO", () => {
        expect(true).toBeTruthy();
      });
    });
  });
});
